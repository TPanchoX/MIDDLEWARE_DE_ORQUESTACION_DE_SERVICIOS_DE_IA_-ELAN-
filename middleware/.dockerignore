# Excluir del contexto de build todo lo que la imagen no necesita.
.venv*
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Registry generado en ejecuciones locales (la imagen no debe incluirlo).
app/models_store/registry.json

# Documentación: no forma parte de la imagen.
docs/
