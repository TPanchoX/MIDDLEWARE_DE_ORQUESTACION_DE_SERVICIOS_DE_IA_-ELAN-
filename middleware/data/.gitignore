# Ignorar los datos generados en tiempo de ejecución (modelos, registry,
# manifests de bootstrap). La estructura de carpetas se conserva vía .gitkeep.
models_store/*
!models_store/.gitkeep
bootstrap_manifests/*
!bootstrap_manifests/.gitkeep
