# Ignore data files generated at runtime (models, registry, bootstrap manifests).
# Keep the directory structure (tracked via .gitkeep files).
models_store/*
!models_store/.gitkeep
bootstrap_manifests/*
!bootstrap_manifests/.gitkeep
